<!DOCTYPE html>
<html>
	<head>
		<title>KitsuneUdon</title>
		<%= render 'layouts/stylesheets' %>
		<%= stylesheet_link_tag 'custom', :media => 'screen' %>
		<%= javascript_include_tag :defaults %>
		<%= csrf_meta_tag %>
	</head>
<body>
	<div class="container">
		<section>
			<%= yield %>
		</section>
		<%= render 'layouts/footer' %>
	</div>
</body>
</html>
